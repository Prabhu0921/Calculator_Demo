//This program is to demonstarte the GIT using calculator program as an example.
#include <stdio.h> 

int main()
{
    printf("Welcome to Calcualtor");
    printf("Please select the operation:");
    sum(a ,b); //new sum function
    multiplication(a,b,c);

return(0);
}